# Harvests PRONOM and builds skeleton suites

// make sure configured with git config --global push.followTags true

- update DROID and container signature files
- tag e.g.
git tag -a v91 -m "PRONOM v91 release"
